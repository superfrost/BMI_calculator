name: weight_calc
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.0  #flutter pub run flutter_launcher_icons:main
  flutter_native_splash: ^1.1.5+1 #flutter pub run flutter_native_splash:create

flutter_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: assets/BMI_ico_splash.png

flutter_native_splash:
  color: "#000000"
  image: assets/BMI_ico_splash.png
  web: false
  #color_dark: "#042a49"
  #image_dark: assets/splash-invert.png


flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
